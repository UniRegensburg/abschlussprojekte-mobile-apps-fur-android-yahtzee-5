<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/light_green</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    //Actionbar wird durchsichtig, Icons werden aber noch angezeigt
    <style name="AppTheme.ActionBar.Transparent" parent="AppTheme">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="colorPrimary">@android:color/transparent</item>
    </style>

//ausgewähltes Item im Burgermenü wird dunkelgrün eingefärbt und hat einen hellgrünenn Hintergrund
    <style name="navigationTheme">
        <item name="colorPrimary">@color/dark_green</item>
    <item name="colorControlHighlight">@color/light_green</item>
    </style>

</resources>
